fn main() {
    let x = 1;

    // this will throw an error because rust have strict "type safety"
    x = 'hello';
    println!("Hello, world!!")
}

// it is system language -> it is very close to system, it can access the system
// tools efficiently so it is called low-level language

// rust lets us use threads which can access multiple cores for running unlike node which is single threaded (runs only on 1 of the core of the cpu)

